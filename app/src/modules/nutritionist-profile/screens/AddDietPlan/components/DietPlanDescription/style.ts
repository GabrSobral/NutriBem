import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  dateButton: {
    paddingHorizontal: 12,
    paddingVertical: 8,
    borderWidth: 1,
    borderRadius: 8,
    flexDirection: "row",
    gap: 12,
    alignItems: "center",
  },
  buttonContainer: {
    flexDirection: "row",
    gap: 8,
    alignItems: "center",
    flex: 1,
    marginTop: 8,
  },
});
